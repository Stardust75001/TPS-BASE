name: CI Shopify Site

on:
  pull_request:
    branches: [ main ]
  push:
    branches: [ main ]

jobs:
  basic-build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "basic-build ok"

  smoke-tests:
    needs: basic-build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "smoke-tests ok"

  theme-check:
    needs: basic-build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "theme-check ok"
