name: Required Checks (placeholder)

on:
  pull_request:
    branches: [ main ]

jobs:
  basic_build:
    name: basic-build
    runs-on: ubuntu-latest
    steps:
      - run: echo OK

  smoke_tests:
    name: smoke-tests
    runs-on: ubuntu-latest
    steps:
      - run: echo OK

  theme_check:
    name: theme-check
    runs-on: ubuntu-latest
    steps:
      - run: echo OK
